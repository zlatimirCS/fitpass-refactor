'use client';

const Loader = () => {
  return (
    <section className='topBottomSpacing firstSection'>
      <div
        className='wrapper'
        style={{ display: 'flex', justifyContent: 'center' }}
      >
        <div className='loader'></div>
      </div>
    </section>
  );
};
export default Loader;
